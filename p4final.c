#include<stdio.h>
#include<math.h>

int input()
{  int n;
  printf("Enter the value of n: \n");
  scanf("%d",&n);
 }

int find_fibo(int n)
{  
  if(n==1)
    return 0;
  else if(n==2)
    return 1;
   else 
    int a=0,b=1,sum
  for(int i=1;i<n;i++)
    {
      sum = a+b;
    }    
}
